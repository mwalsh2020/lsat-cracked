<div class="py-5">
  <div class="pt-5 container">
    <% if policy([:guest, @intro_quiz_session]).create? %>
      <%= render "guest/quiz_sessions/form", quiz_session: @intro_quiz_session %>
    <% end %>
  </div>
</div>
