<%= form_tag orders_path do |f| %>
  <%= hidden_field_tag :orderable_id, orderable.id %>
  <%= submit_tag "Buy now", class: "btn btn-block #{orderable.highlight? ? "btn-lg btn-link" : "btn-link text-white"}" %>
<% end %>
