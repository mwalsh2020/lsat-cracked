<div class="container pt-5">
  <h1>New question for <%= @section.title %></h1>
  <%= simple_form_for [:admin, @section, @question] do |f| %>
    <%= f.rich_text_area :content %>
    <%= f.button :submit, class: "btn btn-primary" %>
  <% end %>
</div>
