<ol class="p-0">
  <%= simple_form_for [:course, quiz_session.quiz, quiz_session], html: { class: "quiz-answer" } do |f| %>
    <%= f.simple_fields_for :quiz_questions do |fqq| %>
      <li>
        <%= fqq.object.question.prompt %>
        <% if fqq.object.question.premium? %>
          <span class="badge badge-secondary" data-toggle="tooltip" title="This question appeared in a real LSAT exam">LSAT Official</span>
        <% end %>
        <div class="list-group pt-4">
          <%= fqq.association :answer,
              collection: fqq.object.possible_answers,
              wrapper: :radio_list_group,
              as: :radio_buttons,
              label: false %>
        </div>
      </li>
      <hr class="mb-4">
    <% end %>
    <div class="text-right"><%= f.button :submit, "Finish quiz", class: "btn-primary btn-lg" %></div>
  <% end %>
</ol>
