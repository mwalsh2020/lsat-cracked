<ul>
  <% chapters.each do |chapter| %>
    <li>
      <% if policy(chapter).show? %>
        <h2><%= chapter.title %></h2>
      <% else %>
        <h2>
          <%= chapter.title %>
          <small class="badge badge-primary" style="font-size: 0.875rem">Premium</small>
        </h2>
      <% end %>
      <%= render "course/sections/menu", sections: chapter.sections %>
    </li>
  <% end %>
</ul>
